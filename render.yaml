services:
  - type: web
    name: pharmaguard-api
    runtime: docker
    rootDir: backend
    envVars:
      - key: OLLAMA_BASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.11"
