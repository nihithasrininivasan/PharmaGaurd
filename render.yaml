services:
  - type: web
    name: pharmaguard-api
    runtime: docker
    rootDir: backend
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11"
